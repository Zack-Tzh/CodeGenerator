package ${pojoPackage};

import java.io.Serializable;
${importPackage}

/**
 * @title: ${className}
 * @projectName: ${projectName}
 * @description: TODO
 * @author: ${author}
 * @date: ${date}
 */
${annotation}
public class ${className} implements Serializable{

${body}
}
